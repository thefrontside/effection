# @effection/devtools-hooks

This package adds hooks to an application that can be used to attach the
Effection devtools.

This package is usually not used directly, but rather through the
`@effection/inspect` package.
